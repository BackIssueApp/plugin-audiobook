{
  "name": "audiobooks",
  "version": "0.3.2",
  "description": "Audiobook library: an Audiobooks library type synced on demand from a remote source, streamed (range proxy) rather than downloaded, with an in-browser player — resume, chapters, playback speed, sleep timer, bookmarks, and per-user listening stats",
  "type": "module",
  "private": true,
  "license": "GPL-3.0-or-later",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --test"
  }
}
